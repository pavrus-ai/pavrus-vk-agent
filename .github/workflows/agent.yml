

name: pavrus-vk-agent

on:
  workflow_dispatch: # ручной запуск для проверки
  schedule:
    - cron: "0 7 * * *" # ежедневно 07:00 UTC = 10:00 МСК

permissions:
  contents: write # чтобы коммитить history.json (защита от повторов)

jobs:
  publish:
    runs-on: ubuntu-latest
    env:
      VK_TOKEN: ${{ secrets.VK_TOKEN }}
      VK_GROUP_ID: ${{ secrets.VK_GROUP_ID }}
      TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
      TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: pip install requests
      - name: Запуск агента
        run: python agent.py
      - name: Сохранить историю
        run: |
          git config user.name "pavrus-agent"
          git config user.email "agent@users.noreply.github.com"
          git add history.json 2>/dev/null || true
          git diff --cached --quiet || { git commit -m "history update"; git push; }
